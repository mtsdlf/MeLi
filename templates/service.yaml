apiVersion: v1
kind: Service
metadata:
  name: {{ include "disk-app-chart.fullname" . }}
  labels:
    {{- include "disk-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: tcp
  selector:
    {{- include "disk-app-chart.selectorLabels" . | nindent 4 }}
